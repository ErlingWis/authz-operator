---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: authorizationmodules.auth.bridder.io
spec:
  group: auth.bridder.io
  names:
    kind: AuthorizationModule
    listKind: AuthorizationModuleList
    plural: authorizationmodules
    singular: authorizationmodule
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: AuthorizationModule is the Schema for the authorizationmodules
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AuthorizationModule
            properties:
              permissions:
                additionalProperties:
                  description: AuthorizationPermission defines a computed relationship.
                  properties:
                    anyOf:
                      description: anyOf grants the permission when any referenced
                        relation applies.
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - anyOf
                  type: object
                description: permissions define computed relationships derived from
                  roles or other permissions.
                type: object
              resource:
                description: resource is the OpenFGA object type this module contributes.
                minLength: 1
                pattern: ^[a-z][a-z0-9_]*$
                type: string
              roles:
                additionalProperties:
                  description: AuthorizationRole defines a directly assignable relationship.
                  properties:
                    inherited:
                      description: inherited grants this role through another object
                        relation.
                      items:
                        description: InheritedRelation defines a relation reached
                          through a topology relation.
                        properties:
                          relation:
                            description: relation is the relation to compute on the
                              related resource.
                            minLength: 1
                            pattern: ^[a-z][a-z0-9_]*$
                            type: string
                          via:
                            description: via is the topology relation to follow on
                              this resource. When omitted, relation is inherited from
                              this resource.
                            minLength: 1
                            pattern: ^[a-z][a-z0-9_]*$
                            type: string
                        required:
                        - relation
                        type: object
                      minItems: 1
                      type: array
                    subjects:
                      description: subjects are the object types or usersets that
                        can be directly assigned this role.
                      items:
                        description: AuthorizationSubject defines a type restriction
                          for a directly assignable role.
                        properties:
                          relation:
                            description: relation references a userset relation on
                              the subject type, such as group#member.
                            minLength: 1
                            pattern: ^[a-z][a-z0-9_]*$
                            type: string
                          type:
                            description: type is the OpenFGA subject type, such as
                              user or group.
                            minLength: 1
                            pattern: ^[a-z][a-z0-9_]*$
                            type: string
                        required:
                        - type
                        type: object
                      minItems: 1
                      type: array
                  type: object
                description: roles define directly assignable relationships on this
                  resource.
                type: object
              topology:
                additionalProperties:
                  description: TopologyRelation defines an object relation to other
                    resource types.
                  properties:
                    resources:
                      description: resources are the OpenFGA object types this relation
                        can point to.
                      items:
                        minLength: 1
                        pattern: ^[a-z][a-z0-9_]*$
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - resources
                  type: object
                description: topology defines object relationships to other resource
                  types.
                type: object
            required:
            - resource
            type: object
          status:
            description: status defines the observed state of AuthorizationModule
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the AuthorizationModule resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedModelHash:
                description: observedModelHash is the hash of the compiled authorization
                  model observed by the controller.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
